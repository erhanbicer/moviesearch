import { dimensions as dimen, fontSize } from '@resources/dimensions';
import commonStyles from '@resources/commonStyles';
import { theme } from '@resources/theme';
import { fonts } from '@resources/fonts';

const R = {
  dimen,
  fontSize,
  commonStyles,
  theme,
  fonts,
};

export default R;
